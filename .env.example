DISCORD_TOKEN=
# Only needed when running the optional `npm run deploy` command manually.
# Discord Developer Portal > General Information > Application ID.
DISCORD_CLIENT_ID=
# Optional: set this to register commands in one server immediately.
DISCORD_GUILD_ID=
# Optional persistent path for SkyBucks balances.
ECONOMY_DATA_FILE=./data/economy.json
